# .env.local
VITE_API_BASE_URL=http://localhost:8080

# Ativa o login de desenvolvimento (você já usa: e-mail/senha fixos)
VITE_ENABLE_DEV_LOGIN=true

# MODO MOCK das APIs (escolha UMA das opções abaixo):
# Opção A) Sempre mockar serviços (mesmo com token normal):
VITE_AUTH_MODE=mock

# Opção B) Mockar só quando usar DEV Login (token "dev-*"):
# VITE_ENABLE_DEV_API=true
